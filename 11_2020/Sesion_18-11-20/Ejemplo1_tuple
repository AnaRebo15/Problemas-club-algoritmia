#include <bits/stdc++.h>
using namespace std;

int main(){
	tuple<int, string, string> alumno;
	alumno = make_tuple(1, "Julio", "Perez");
	//Acceso con get
	cout<<get<0>(alumno)<<endl;
	cout<<get<1>(alumno)<<endl;
	cout<<get<2>(alumno)<<endl;
	
	//Declaración de variables
	int num;
	string nombre;
	string apellido;
	tie(num, nombre, apellido) = alumno;
	cout<<num<<" "<<nombre<<" "<<apellido<<endl;
	
	return 0;
}
